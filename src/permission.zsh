#!/usr/bin/env zsh

#
# Permissions
#
  # $1 is the file name
  can_read() {
  # $2 [optional] a specified user in the system or the user running the tests
}

can_write() {
  # $1 is the file name
  # $2 [optional] a specified user in the system or the user running the tests
}

can_execute() {
  # $1 is the file name
  # $2 [optional] a specified user in the system or the user running the tests
}

has_permissions() {
  # $1 is the file name
  # $2 [optional] a specified user in the system or the user running the tests
}

has_permission_gt() {

}

has_permission_lt() {

}
